const initialState = {
  song: {},
  loading: false,
  error: null,
};

export default function currentSongRegucer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
